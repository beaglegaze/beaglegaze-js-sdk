name: CI

on:
  push:
    branches: [ * ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20.x, 22.x]

    steps:
    - uses: actions/checkout@v3
      with:
        submodules: false
        token: ${{ secrets.GITHUB_TOKEN }}
    - name: Checkout submodules manually
      run: |
        git config --file .gitmodules submodule.contracts.url https://github.com/beaglegaze/beaglegaze-web3.git
        git submodule sync
        git submodule update --init --depth=3 contracts
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}
    - name: Build custom Docker image
      run: docker build -t hardhat-testnet contracts/eth-dev-node/.
    - name: Install dependencies
      run: npm install
    - name: Build
      run: npm run build --if-present
    - name: Run tests
      run: npm run test
